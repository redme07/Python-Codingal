numerator = float(input("Enter a number that will be a numerator"))

denominator = float(input("Enter a denominator for the numerator"))

if(numerator % denominator == 0):
    print("The numerator is divisible by the denominator")

else:
    print('The numerator is not divisible by the denominator')