text = str(input("Enter any text, word or number"))
#this reverses the text using negative indexation
reverse_text = text[::-1]
#When using indexations the space before the first index is for the starting, the space 
#before the second is for the ending index and the space after the second colan is for the step
print(reverse_text)